import { StartsWith, EndsWith, MultipleOf } from './decorators';
export { mapToClassValidators, decoratorsFor } from './mapper';
export const decorators = {
  StartsWith,
  EndsWith,
  MultipleOf
};
export { StartsWith, EndsWith, MultipleOf };
